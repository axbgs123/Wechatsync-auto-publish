# 文章同步助手 2.0.9｜自动发布集成版

## 项目概览

**文章同步助手 2.0.9 自动发布集成版**是一套基于 Chrome Extension、TypeScript 和 MCP
协议构建的多平台内容分发系统。

项目面向需要在多个内容平台同步文章的创作者、编辑和运营人员，解决文章重复复制、平台格式
不一致、草稿状态难以追踪，以及发布失败后无法恢复等问题。

在原有多平台草稿同步能力的基础上，本版本增加了独立发布器，将内容分发流程扩展为：

```text
文章提取 / Markdown / HTML
        ↓
多平台内容预处理
        ↓
创建并登记平台草稿
        ↓
用户预览与明确确认
        ↓
独立发布器提交公开发布
        ↓
状态核验、审核跟踪与失败恢复
```

项目同时提供 MCP 接口，使 Claude Desktop、Claude Code 等兼容客户端可以通过结构化工具
完成草稿创建、草稿查询和确认后的自动发布。

## 项目背景

很多内容创作者需要将同一篇文章分发到微信公众号、知乎、头条、百家号、B 站等平台。不同
平台的编辑器、图片接口、正文格式和发布流程各不相同，传统的复制粘贴方式容易出现：

- 图片丢失或需要重复上传；
- Markdown、HTML 和平台编辑器之间格式不一致；
- 草稿创建成功但本地没有记录；
- 发布请求超时后无法判断平台是否已经提交；
- 重试时重复创建草稿或重复发布；
- 只能人工打开每个平台确认状态。

本项目通过浏览器扩展复用用户已经登录的平台会话，把“草稿同步”和“公开发布”拆分成两个
可追踪、可恢复的阶段。

## 核心功能

### 多平台草稿同步

支持从网页、微信公众号文章、Markdown 或 HTML 内容提取文章，并批量创建多个平台草稿。
每个平台可以使用独立的内容预处理配置，例如链接清理、图片处理、代码块处理、表格转换和
HTML 压缩。

### 独立发布器

草稿同步完成后，用户可以在扩展的“发布器”页面中查看草稿列表、平台、标题、创建时间和
当前状态，并在检查内容后单独执行公开发布。

当前已接入独立发布流程的平台包括：

- 微信公众号
- 知乎
- 今日头条
- 百家号
- B 站专栏
- 搜狐号（待真实账号验证）

### 发布状态与恢复

发布器将平台结果统一归类为已发布、审核中、结果未确认、失败和已阻止等状态。对于网络
中断、浏览器关闭或平台响应不明确的情况，系统会保留本地发布记录，并在重试前先核验平台
状态，降低重复发布风险。

### MCP 自动发布

MCP 客户端可以调用以下工具：

| 工具 | 用途 |
|---|---|
| `list_platforms` | 查询支持的平台和登录状态 |
| `check_auth` | 检查指定平台是否已登录 |
| `sync_article` | 创建一个或多个平台草稿 |
| `list_drafts` | 查询草稿 ID、平台和当前状态 |
| `publish_draft` | 在 `confirmed: true` 后执行公开发布 |
| `extract_article` | 从当前浏览器页面提取文章 |
| `upload_image_file` | 上传本地图片并返回可访问 URL |

MCP 的发布流程保持显式确认：

```text
sync_article → list_drafts → 用户检查草稿
             → publish_draft(confirmed: true)
             → published / reviewing / failed / blocked
```

`sync_article` 只创建草稿，不会直接公开发布；只有用户确认后调用 `publish_draft` 才会
执行公开发布动作。

## 当前支持的平台

公开源码当前注册 20 个内置适配器，并支持 WordPress、Typecho 等 CMS 账号：

| 平台 | 草稿同步/导出 | 独立自动发布 |
|---|---:|---:|
| 微信公众号 | ✅ | ✅ 已接入 |
| 知乎 | ✅ | ✅ 已接入 |
| 今日头条 | ✅ | ✅ 已接入 |
| 百家号 | ✅ | ✅ 已接入 |
| B 站专栏 | ✅ | ✅ 已接入 |
| 搜狐号 | ✅ | ⏳ 待验证 |
| 掘金 | ✅ | — |
| CSDN | ✅ | — |
| 微博 | ✅ | — |
| 语雀 | ✅ | — |
| 豆瓣 | ✅ | — |
| 雪球 | ✅ | — |
| 东方财富 | ✅ | — |
| 人人都是产品经理 | ✅ | — |
| 51CTO | ✅ | — |
| 慕课网 | ✅ | — |
| 开源中国 | ✅ | — |
| SegmentFault | ✅ | — |
| 博客园 | ✅ | — |
| Markdown ZIP 导出 | ✅ | 不适用 |

此外，系统支持 WordPress API 和 MetaWeblog 协议，可用于 WordPress、Typecho 等自建站点。

## 系统架构

```text
┌──────────────────────────────────────────────┐
│ Chrome Extension                             │
│                                              │
│  Popup / Editor                              │
│       ↓                                      │
│  Background Service Worker                   │
│       ├─ 内容预处理与平台调度                │
│       ├─ 草稿登记与同步历史                  │
│       └─ 独立发布器与发布审计                │
│              ↓                               │
│       Core Platform Adapters                 │
│              ↓                               │
│  微信 / 知乎 / 头条 / 百家号 / B站 / CMS 等   │
└──────────────────────────────────────────────┘
                 ↑
          WebSocket Bridge
                 ↑
┌──────────────────────────────────────────────┐
│ MCP Server / CLI                              │
│  sync_article → list_drafts → publish_draft  │
└──────────────────────────────────────────────┘
```

项目按职责拆分为四个主要模块：

- `packages/core`：统一类型、运行时接口、文章处理和平台适配器；
- `packages/extension`：Chrome 扩展、页面提取、同步调度、草稿登记和独立发布器；
- `packages/mcp-server`：MCP 工具定义、参数校验和 WebSocket 桥接；
- `packages/cli`：本地命令行入口和文章同步调用。

## 关键工程设计

### 草稿与公开发布解耦

原有同步协议只负责创建草稿。本版本新增独立的 `publishDraft` 流程，不改变原有
`syncArticle` 的语义，既保持旧调用方兼容，也让用户可以在公开发布前再次检查内容。

### 幂等控制

系统使用文章内容摘要、平台草稿标识和发布幂等键识别重复操作：

- 相同文章和平台优先复用未发布草稿；
- 同一草稿不会并发提交多个发布请求；
- 已发布草稿再次操作会被阻止；
- 发布异常后先核验平台状态，再决定是否重试。

### 平台适配器架构

每个平台通过统一的 Adapter 接口实现认证、文章发布、图片上传和结果返回，同时保留平台
自己的内容预处理配置。这样新增平台时不会影响 MCP、草稿记录和发布器的公共逻辑。

### 浏览器登录态复用

扩展直接使用用户在 Chrome 中已有的平台登录状态，不在项目源码或日志中保存平台密码、
Cookie 或 Token。MCP Token 只用于 MCP Server 与浏览器扩展之间的桥接鉴权。

## 测试与验证

项目包含针对核心适配器和扩展发布流程的自动化测试，覆盖：

- 草稿创建和结果标准化；
- 草稿登记与重复同步复用；
- 发布幂等键和重复发布拦截；
- 发布中断、审核中和失败恢复；
- MCP 发布参数与确认逻辑；
- 微信公众号封面和草稿接口；
- 知乎发布器；
- 今日头条、百家号等平台适配器；
- 消息来源、标签页和发布状态保护。

当前验证结果：

- Core 自动化测试：35 项通过；
- Extension 自动化测试：73 项通过；
- Core、Extension、MCP Server、CLI TypeScript 类型检查通过。

## 项目截图

建议在作品集网站展示以下三张截图：

1. 扩展首页：展示平台选择、文章识别和批量同步入口；
2. 独立发布器：展示草稿列表、发布状态、预览和确认发布按钮；
3. MCP 调用结果：展示草稿创建、草稿 ID、发布确认和状态返回。

截图文件可以放在项目目录的 `portfolio-assets/` 下，并按下面的方式替换：

```md
![扩展首页](./portfolio-assets/extension-home.png)
![独立发布器](./portfolio-assets/publisher.png)
![MCP 自动发布](./portfolio-assets/mcp-publish.png)
```

## 项目成果

- 将单纯的多平台草稿同步扩展为可确认、可追踪、可恢复的发布系统；
- 将 MCP 从“同步文章”扩展到“草稿管理 + 用户确认 + 自动发布”；
- 统一多个平台的草稿记录、发布状态和错误处理方式；
- 降低重复创建草稿和重复公开发布的风险；
- 在不破坏旧同步协议的前提下接入独立发布能力；
- 通过模块化适配器支持后续继续扩展平台。

## 技术栈

TypeScript、React、Chrome Extension Manifest V3、MCP、WebSocket、Node.js、Vite、Vitest、
Zustand，以及面向多平台内容分发的 Adapter 架构。

## 项目链接

- GitHub：[axbgs123/Wechatsync-auto-publish](https://github.com/axbgs123/Wechatsync-auto-publish)
- 原项目：[wechatsync/Wechatsync](https://github.com/wechatsync/Wechatsync)
- 许可证：[GNU GPL-3.0](./LICENSE)
